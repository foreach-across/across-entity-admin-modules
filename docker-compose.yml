version: "3.1"
services:
  frontend:
    build:
      context: .
      dockerfile: ./docker/Dockerfile.assets
    image: bootstrap-ui-module-assets
    command: gulp
    container_name: bootstrap-ui-module-assets-builder
    volumes:
    - ./frontend:/build
    - ./bootstrap-ui-module/src/main/resources/views/static/BootstrapUiModule:/build/resources/views/static/BootstrapUiModule