management:
  endpoints:
    web:
      exposure:
        include: '*'

spring:
  liquibase:
    enabled: false
  main:
    allow-bean-definition-overriding: true
    allow-circular-references: true

acrossHibernate:
  create-unit-of-work-factory: true

adminWebModule:
  title: Admin Web Themes
  login:
    rememberMe:
      key: test
      cookie: rm-awm-bs4

server:
  port: 8081
